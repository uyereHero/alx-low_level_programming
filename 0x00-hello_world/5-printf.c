#include <stdio.h>
/**
 * main - Entry point (code that prints a statement)
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\g");
	return (0);
}
